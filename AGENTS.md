# AGENTS.md

## Build instruction

For this repository, always use the following script for builds:

```bash
firmware/dual_throttle_build.sh
```

When operating from the repository root, run:

```bash
cd firmware && ./dual_throttle_build.sh
```

Do not use `lite_build.sh` unless the user explicitly asks for the lite variant.
